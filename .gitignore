__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
reports/
.coverage
coverage.xml
htmlcov/

# Android build outputs (android/ scaffold)
android/.gradle/
android/local.properties
android/app/build/
android/build/
android/.idea/

# per-ABI wheels for the compiled flavor (build output)
android/app/pysrc/

# scratch tree for android_wheel.py: cythonised copies, the fetched Android
# CPython, and a cached reference wheel.  It was committed by accident once,
# and the cached wheel then froze the compiled flavour's metadata at that
# version — see CHANGELOG 0.7.7.
build-android-wheel/

# setuptools staging dir, likewise committed by accident once (0.7.5)
/build/
