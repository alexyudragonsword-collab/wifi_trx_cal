# 待办清单(backlog)

## B4. RX DC 两级校正 —— ✅ 已落地(2026-08-01)

- **落地内容**:`RxChain` 增加逐档**模拟 DC 微调 DAC**(基带节点减除,±0.064 √mW / 2e-3 步进 ≈6-bit,硬件量化建模);`rx_dc` 校准两级化——固定安全 VGA(20 dB,保证最差原始 DC 不打饱和 ADC)下先测原始 DC 编程模拟粗调,再测残余编程数字细调;**两级校正均按基带节点参考存储、运行时按当前 VGA 换算**(修掉了旧数字表与校准时 VGA 绑定的隐性缺陷)。
- **验收实测**:高 VGA 场景(20M,−90 dBm,无噪声)从 NaN/失锁恢复到干净锁定(−60 dB EVM);**20M 低 MCS 灵敏度首次可测且与 Friis 闭合**:MCS0 −95.8(Δ−0.3)/MCS2 −90.3(Δ−0.8)/MCS4 −83.9(Δ−1.0);全带宽 14/14,粗调后 DC ≤ −55 dBFS、终值 ≤ −83 dBFS;逐档校准自适应 8 档。cal-state JSON `corrections` 新增 `dc_ana` 字段(`dc_post` 语义改为节点参考细调)。

## A3. 8 档 AGC 正式化 —— ✅ 已落地(2026-08-01)

- **落地内容**:系统组调整稿(state 1 NF 6/IIP3 −14、state 3 IIP3 −4、state 5 IIP3 +6、state 7 NF 38→34(2026-08-01 二次调整,恰使 t6=−13.0 精确落在平衡点),门限沿用提案值)写入 `chain/agc.py`;`CAL_OBSERVATION_STATE` 迁移到 NF=22 档(index 4);逐档 DC 先验扩到 8 档。
- **落地实测**(seed 5,全带宽 14/14):20M/1024 tx −42.7 / 环回 −43.1;80M −43.2/−42.6;320M/4096 tx −42.8 / 环回 −40.9;**320M 工作点 RX EVM −36.6 → −40.0**(跌档带被细分档消除);RX 分解:强信号区 −38.1 ~ −41.5,噪声/IM3 交替主导。
- **备查**:按当前参数重解的平衡门限与沿用值差 ≤1.3 dB(t0 −41.0 / t1 −35.7 / t3 −25.0 / t5 −15.7 / t6 −11.7),如需可再收 ~0.5 dB 纹波;门限按带宽分表(80M/20M 常数 −95/−101)仍为可选优化。

## 历史:A1 门限-耦合-钉档首轮落地与 4 档→8 档演进(数据收集中)

- (下表与派生分析保留作决策记录)
- **提案表**(锚点档 0/2/4/6 为电路数据;插值档 1/3/5 为假设值,待调):

  | # | 增益 | NF | IIP3 | 门限(320M 平衡) |
  |---|---|---|---|---|
  | 0 | 37 | 3.5 | −20 | −40.7 |
  | 1 | 31 | 6.75* | −14.5* | −36.0 |
  | 2 | 25 | 10 | −9 | −30.3 |
  | 3 | 19 | 16* | −4.5* | −25.3 |
  | 4 | 13 | 22 | 0 | −21.0 |
  | 5 | 7 | 26* | +4* | −17.0 |
  | 6 | 1 | 30 | +8 | −13.0 |
  | 7 | −5 | 34* | +12* | +10(末档;外推档,2026-08-01 增补) |

- **门限公式**:tᵢ = (2·IIP3ᵢ + NFᵢ₊₁ + (−174+10·log₁₀BW)) / 3;建议落地为按带宽门限表(320M/80M/20M 常数 −89/−95/−101)。
- **实测依据**(320M,seed 5):4 档平衡门限最差 −37.4/纹波 4.5 dB;7 档插值 −39.2/1.5 dB,贴 MCS13 线;连续 AGC 包络极限 ≈ −40.5,由 ⅔(IIP3ᵢ−NFᵢ₊₁+89) 决定——突破需降中间档 NF(如 state 3 的 16→13 可再抬 ~1.5 dB)。
- **落地注意**:校准观测钉档从 state 2 改为新表 NF=22 档(state 4);`CAL_OBSERVATION_STATE` 同步;RX DC/per-state IQ 等逐档校准的档数自适应(代码已按 `len(lna_states)` 写)。

维护约定:每项标注 **状态**(待外部输入 / 可立即做)、来源与携带数据;完成后从本文件删除并在提交信息中引用。会话任务列表与本文件同步,本文件为唯一权威来源。

## A. 等外部输入(blocked)

### A1. AGC 档位表统一修订 —— ✅ 已落地(2026-08-01)
- **落地内容**:新档位表(37/3.5/−20/−47、25/10/−9/−36、13/22/0/−27、1/30/+8/+10)写入 `chain/agc.py`;校准观测钉 state 2(`RxChain.agc_pinned` + `CAL_OBSERVATION_STATE`,DPD/IQ/快照观测生效;IIP2 的热双音保留正常选档由末档护线性);耦合衰减按带宽取推荐值(320M→34 dB,`recommended_loopback_atten_db`);IIP2 子路径衰减钳 ≥30 dB;DPD 观测固定用冷耦合点(≥40 dB,防 RX IM3 被学进预失真器);DPD ACLR 回归闸门加 −45 dBc 底线。VGA 保持 −10~+40。
- **落地后实测**(seed 5,14/14 全过):20M/1024 tx −42.7 / 环回 −43.1;80M/1024 −43.2/−42.6;320M/4096 **tx −42.75 / 环回 −40.8**(spec −38 余量 4.75 dB)。纸面预测全部命中:环回观测底板 ~−42(NF22+IIP3 0 联合受限)、320M 工作点(−22.5 dBm)的 RX EVM 读数 −36.6 即 state 3 NF=30 跌档带的真实体现。
- **遗留观察**(信息记录,非缺陷):320M 中强输入区(−27~−19 dBm)MCS11/13 跌档带,建议与链路组对齐 AGC 策略(高 MCS 场景限制进 state 3);20M 环回 −43.1 对测试阈值 −42 余量收窄到 1.1 dB,seed 敏感性若出现按新底板重锚。
- **已收到的修订数据**:

  | state | 增益 (dB) | NF (dB) | IIP3 (dBm) | 切换门限 | 备注 |
  |---|---|---|---|---|---|
  | 0 | 36 → **37** | 4.5 → **3.5** | −12 → **−20** | 待定 | 2026-08-01 收到 |
  | 1 | 24 → **25** | 6.5 → **10** | −2 → **−9** | 待定 | 2026-08-01 收到 |
  | 2 | 12 → **13** | 10 → **22** | +6 → **0** | 待定 | 2026-08-01 收到(NF 与此前确认一致) |
  | 3 | 0 → **1** | 16 → **30** | +14 → **+8** | 待定 | 2026-08-01 收到,**四档数据齐** |

- **state 0 新值的派生提示**(纸面推算,未仿真):IIP3 从 −12 降到 −20 dBm 后,若维持现切换门限 −42 dBm,边界 IM3 = 2×(−20−(−42)) = **44 dBc**,低于 54 dBc 准则——要么门限下移到 ≤ **−47 dBm**,要么接受该档贴边界处的 EVM 锯齿;NF 3.5 使灵敏度改善 1 dB;增益 +1 dB 使 VGA 低端上轨点从 −86 移到 −87 dBm。门限决策待各档数据齐后统一定。
- **state 1 新值的派生提示**(纸面推算,未仿真):
  - 若维持现门限 −30 dBm,边界 IM3 = 2×(−9−(−30)) = **42 dBc**;满足 54 dBc 需门限 ≤ **−36 dBm**。注意与 state 0 的门限联动:若 0→1 门限下移到 −47,state 1 窗口变为 (−47, −36],窗口底部输入更低、对 NF 更敏感;
  - NF 6.5 → **10**:0→1 切换瞬间 SNR 跳降从 2 dB 变为 **6.5 dB**。窗口底部热 SNR(以 −47 dBm 入、NF 10 计):20M 54 dB / 80M 48 dB / **320M 42 dB——距 MCS13 的 40 dB 只剩 2 dB**,320M 高阶 MCS 在 0→1 切换点附近可能出现速率跌档区,门限规划时需与链路组对齐;
  - 增益 24→25:VGA 分配整体 −1 dB,无轨道问题。
- **state 2 新值的派生提示**(纸面推算,未仿真):
  - IIP3 从 +6 掉到 **0 dBm**:维持现门限 −18 dBm 时边界 IM3 = 2×(0−(−18)) = **36 dBc**(现档位表中最差);满足 54 dBc 需门限 ≤ **−27 dBm**。与 state 1 联动后窗口约为 (−36, −27];
  - **与 320M 环回观测恢复方案冲突,需重新联合求解**:此前"NF=22 → 耦合衰减降到 32~34 dB"的方案把 RX 输入抬到 −20.5~−22.5 dBm,前提是仍落在 state 2——若 2→3 门限按上一条下移到 −27,该输入将落入 state 3(NF/IIP3 待收)。环回工作点(现 −28.5 dBm,新窗口内距顶 1.5 dB,IM3 = 57 dBc 尚可)与 320M 衰减设计点必须等 state 3 数据到齐后与门限一起定;
  - NF 22 的影响已量化(见 A1 状态行);增益 12→13 无轨道问题。
- **state 3 新值的派生提示 + 全表联合分析**(纸面推算,未仿真):
  - 新阶梯:增益 37/25/13/1(均匀 12 dB 步进,VGA ±范围无压力);
    NF 3.5/10/22/30;IIP3 −20/−9/0/+8。总体走向:高增益档更好
    (NF −1 dB、灵敏度 +1 dB),中低增益档线性度与 NF 全面变差;
  - **按"边界 IM3 ≥ 54 dBc"反推的门限方案**:0→1 ≤ −47、
    1→2 ≤ −36、2→3 ≤ −27 dBm;state 3 作为末档,−19 dBm 以上
    IM3 必然 <54 dBc(IIP3 +8 的物理上限),0 dBm 处仅 16 dBc,
    强信号端 EVM 将由 IM3 主导;
  - **state 3 NF=30 的高阶 MCS 缺口**:2→3 切换的 SNR 跳降达 8 dB;
    320 MHz 下 state 3 窗口底部(−27 dBm)热 SNR 仅 32 dB——
    MCS11(34.5)/MCS13(40)在 −27~−19 dBm 区间收不了,
    **中强输入区存在高阶 MCS 跌档带**,需与链路组对齐;
  - **320M 环回观测的联合求解(关键)**:IIP3 从 +6 掉到 0 使 state 2
    的观测线性度余量缩了 12 dB。钉住 state 2(校准模式 AGC pin,
    真实芯片惯例)并联合优化耦合衰减:热 SNR(p+67)与 IM3(−2p)
    在 p ≈ **−22 dBm(atten ≈ 33~34 dB)**相交于 ~44.5 dB,
    联合观测底板 ≈ **−42 dB**——环回 EVM 预计 ~−41,对 −38 spec
    余量收窄到 ~3 dB;此前"NF22 + atten 32~34 全步通过"的实测是在
    旧 IIP3(+6)下取得的,新 IIP3 下各校准步骤健康度**必须等落地
    仿真确认**;
  - VGA 低端上轨:state 0(增益 37 + VGA 40)在 p_in < −87 dBm 后
    ADC 欠驱,而新 NF 3.5 使 20M 低 MCS 灵敏度更深(MCS0 ≈
    −95.5 dBm),**灵敏度测量的 VGA 欠驱区扩大**——落地时决定:
    抬 VGA 上限或接受欠驱(真实芯片常以数字增益补偿)。
- **已量化的输入数据**(修订时直接使用):
  - state 2(12 dB 增益)实际 NF ≈ 22 dB(现模型 10 dB):20 MHz 校准与环回观测**无感**(环回 −44.2→−43.2);**320 MHz 观测热底塌到 −39.3**,环回读数失效、rx_iq 校准失败——恢复条件:环回耦合衰减从 40 dB 降到 **32~34 dB**(输入 −20.5~−22.5 dBm 仍在 state 2,IM3 ≥ 53 dBc)。
  - 切换门限建议:各档在自身门限处的 IM3 = 2×(IIP3 − max_input),现档位表为 60/56/48/8 dBc(state 0–3);实测 state 2 贴门限时 EVM 恶化 +1.4 dB。建议按"边界 IM3 ≥ RX 底板 + 10 dB(≈54 dBc)"反推门限。
  - 验收视图:GUI "RX EVM vs input power"(紫色点划线即切换门限,锯齿深度直接可读)。
- **落地范围**:`chain/agc.py` 档位表;环回衰减默认值(320M);灵敏度/链路预算研究重跑;相关测试与交付文档数字重锚定。

### A2. MCS 所需 SNR 列与链路仿真对表
- **状态**:等链路仿真组提供各 MCS 的实测门限(PER 仿真值,含实现损耗)。**这是目前唯一未完成项。**
- **背景**:`link/mcs.py` 的 `snr_req_db` 列是 AWGN 工程近似(模块 docstring 已注明验证状态);MCS13 的 40 dB 直接决定 4096-QAM 灵敏度与 RX 图上需求线的位置。行间差值可信,绝对值需对表。
- **附带发现**:320 MHz MCS13 实测灵敏度比解析值差 +2.6 dB——底板(−43.6)与需求线(−40)贴近导致穿越区偏离纯噪声斜率,4096-QAM 灵敏度是"底板+噪声"联合受限(`sensitivity_study` 已以 `floor_limited` 标记);AGC/NF 修订后此档需复核。

## B5. 基带段(LPF/VGA)噪声与非线性 —— ✅ 已落地(2026-08-02)

- **按基带的口径建模,不用 NF**:`impairments/baseband.py:BasebandStage` 以
  **输入参考噪声电压密度**(默认 6 nV/√Hz @基带节点)与**输出摆幅**
  (默认 1.0 Vpp ⇒ OP1dB +3.98 dBm @50 Ω)描述,另提供
  `from_rms_noise(v_rms, bw)` 接受积分噪声电压;dBm 只是内部表示。
  两个默认值是**按现有 8 档表反推的占位值,非电路数据**,替换各一行。
- **位置不对称是物理**:噪声注入在 **LPF 之前**(信道滤波器像硬件一样整形它),
  压缩施加在 **VGA 之后**(输出参考天花板)。
- **默认关**:开启需先用 `link.budget.deembed_states` 把官方表(级联总值)拆成
  RF-only 档位,否则噪声重复计算;拆解-再级联往返误差 0.0000 dB,
  基带占比 0.07 dB(state 0)→1.11 dB(state 7)。基带噪声若超过某档总值,
  函数直接报错点名该档(与档位表矛盾是结论,不是舍入)。
- **纠正了 B5 原始记录的一条前提**:"VGA 增益设置对 NF 的影响"**本来就已经建模**
  ——ADC 噪声在 VGA 之后,折算回输入时随 VGA 增益变化(state 7 从 VGA 40→0 dB
  时 NF 34.00→34.96)。基带噪声在 VGA **之前**,原理上不可能带来 VGA 相关的 NF;
  实测拆解前后 NF-vs-VGA 曲线逐点相同。真正随 VGA 变的是**线性度**。
- **新增的物理**(实测):
  1. 输入参考 IIP3 随 VGA 增益 **1 dB/dB 塌陷**(state 7:VGA 0/20/40 dB 下
     +11.1/−1.6/−21.4 dBm;lumped 模型恒为 +12.0 dBm);
  2. AGC 把 VGA **输出**钉在固定电平,所以输出天花板的 EVM 代价**与天线功率无关**
     ——实测 −40 dBm 与 −20 dBm 处均为 +2.55/+2.56 dB(−60 dBm 处仅 +0.08 dB,
     因为那里热噪声主导、RSS 上体现不出来)。
  3. 因此 **`adc_backoff_db` 从此有价**:160 MHz/1024-QAM、−40 dBm 输入下,
     lumped 模型最优 backoff ≈10 dB(−43.4 dB)且此后平坦;带 1.0 Vpp 天花板后
     最优推到 ≥18 dB(−42.8 dB),当前的 12 dB 要多付约 2.6 dB EVM。
     加大 backoff 与 ADC 量化噪声之间的折中曲线由 `link/baseband_study.py`
     的 `backoff_study` 现算。
- **验证**:`tests/test_baseband_stage.py`(10 项,含单位换算对拍手算值、
  拆解往返、"NF 不因此获得 VGA 相关性"的反向断言、默认关时逐位一致)。

## B. 可立即做

### B5(原始记录,保留作对照)
- **状态**:待用户排期(2026-08-01 记录,暂不实现)。
- **现状**:RX 的噪声与非线性全部集中在逐 AGC 档的输入参考等效参数里
  (`rx.py` 前端一次性注入:级联 NF 的热噪声 + 档位 IIP3 的无记忆三阶);
  `TunableLPF` 是纯线性 IIR、VGA 是一行增益乘法,均无自身噪声/压缩;
  链路末端唯一的硬非线性是 ADC 削顶/量化。若档位表 NF/IIP3 按
  "LNA+混频器+基带级联到输入端"口径填写(官方 8 档表即此口径),
  基带贡献已隐式包含。
- **未覆盖的三类效应**(lump 近似的边界):
  1. **VGA 增益设置对 NF 的影响**:同一 LNA 档内 VGA 调低时基带噪声
     折算到输入应恶化,现模型 NF 只随 LNA 档变;
  2. **VGA/基带输出压缩**:VGA 自身不压缩,大信号 headroom 只由
     ADC 削顶近似代表(无独立的基带 P1dB);
  3. **LPF 大信号非线性**(带内压缩 / corner 调制):完全没有。
- **建议方案**(实现时二选一或组合):把 NF/IIP3 拆成 LNA 段 + 基带段
  两级级联(基带段随 VGA 设置折算),或给 VGA 加输出参考 P1dB/IIP3;
  逐档校准与 AGC 门限求解已按 `len(lna_states)` 自适应,不受影响,
  但平衡门限公式需改为两段级联版本重推。
- **触发条件**:研究"同档内 VGA 大范围摆动"或"基带限压"场景时再做;
  当前用法(AGC 把 ADC 输入钉在固定 backoff,每档 VGA 落点基本固定)
  下 lump 近似自洽。

<!-- B1(文档同步)、B2(泄漏精修短路)、B3(floor_limited 标记)已完成,
     见提交历史。 -->

