# 待办清单(backlog)

维护约定:每项标注 **状态**(待外部输入 / 可立即做)、来源与携带数据;完成后从本文件删除并在提交信息中引用。会话任务列表与本文件同步,本文件为唯一权威来源。

## A. 等外部输入(blocked)

### A1. AGC 档位表统一修订
- **状态**:数据收集中(逐档确认,全部到齐后一次性落地 + 重锚定;落地前不改 `agc.py`、不跑仿真)。
- **已收到的修订数据**:

  | state | 增益 (dB) | NF (dB) | IIP3 (dBm) | 切换门限 | 备注 |
  |---|---|---|---|---|---|
  | 0 | 36 → **37** | 4.5 → **3.5** | −12 → **−20** | 待定 | 2026-08-01 收到 |
  | 1 | — | — | — | 待定 | 待确认 |
  | 2 | — | 10 → **22**(此前已确认) | — | 待定 | 待确认增益/IIP3 |
  | 3 | — | — | — | 待定 | 待确认 |

- **state 0 新值的派生提示**(纸面推算,未仿真):IIP3 从 −12 降到 −20 dBm 后,若维持现切换门限 −42 dBm,边界 IM3 = 2×(−20−(−42)) = **44 dBc**,低于 54 dBc 准则——要么门限下移到 ≤ **−47 dBm**,要么接受该档贴边界处的 EVM 锯齿;NF 3.5 使灵敏度改善 1 dB;增益 +1 dB 使 VGA 低端上轨点从 −86 移到 −87 dBm。门限决策待各档数据齐后统一定。
- **已量化的输入数据**(修订时直接使用):
  - state 2(12 dB 增益)实际 NF ≈ 22 dB(现模型 10 dB):20 MHz 校准与环回观测**无感**(环回 −44.2→−43.2);**320 MHz 观测热底塌到 −39.3**,环回读数失效、rx_iq 校准失败——恢复条件:环回耦合衰减从 40 dB 降到 **32~34 dB**(输入 −20.5~−22.5 dBm 仍在 state 2,IM3 ≥ 53 dBc)。
  - 切换门限建议:各档在自身门限处的 IM3 = 2×(IIP3 − max_input),现档位表为 60/56/48/8 dBc(state 0–3);实测 state 2 贴门限时 EVM 恶化 +1.4 dB。建议按"边界 IM3 ≥ RX 底板 + 10 dB(≈54 dBc)"反推门限。
  - 验收视图:GUI "RX EVM vs input power"(紫色点划线即切换门限,锯齿深度直接可读)。
- **落地范围**:`chain/agc.py` 档位表;环回衰减默认值(320M);灵敏度/链路预算研究重跑;相关测试与交付文档数字重锚定。

### A2. MCS 所需 SNR 列与链路仿真对表
- **状态**:等链路仿真组提供各 MCS 的实测门限(PER 仿真值,含实现损耗)。
- **背景**:`link/mcs.py` 的 `snr_req_db` 列是 AWGN 工程近似(模块 docstring 已注明验证状态);MCS13 的 40 dB 直接决定 4096-QAM 灵敏度与 RX 图上需求线的位置。行间差值可信,绝对值需对表。
- **附带发现**:320 MHz MCS13 实测灵敏度比解析值差 +2.6 dB——底板(−43.6)与需求线(−40)贴近导致穿越区偏离纯噪声斜率,4096-QAM 灵敏度是"底板+噪声"联合受限(`sensitivity_study` 已以 `floor_limited` 标记);AGC/NF 修订后此档需复核。

<!-- B1(文档同步)、B2(泄漏精修短路)、B3(floor_limited 标记)已完成,
     见提交历史。 -->

