---
type: project_topic
status: active
summary: "守卫会以四种方式失效——空转、错靶、恒红、被遮——而变异验证只抓得住前两种。本文是这四类的判别表、双向验证规程,以及本项目每一次踩中的台账。"
tags: [ci, testing, guards, mutation-testing, process]
contains: [lesson, procedure, decision, experience, open_question]
created: "2026-09-11"
updated: "2026-09-11"
related:
  - docs/backlog_zh.md#B16
  - docs/backlog_zh.md#B26
  - docs/backlog_zh.md#B27
  - docs/backlog_zh.md#B28
  - AGENTS.md
  - README.md
  - .github/workflows/ci.yml
  - tools/docs_staleness.py
  - tests/test_import_layering.py
  - cairn/Android出货线.md
authoring_mode: ai_generated
---
# 守卫有效性(当前真相)

## 形成背景

本项目的正确性主要不是靠人读代码维持的,而是靠守卫:分层守卫、调用面守卫、
金标对拍、端上条数断言、闭环重放、文档与原理图陈旧检查。既然结论都压在守卫
身上,**守卫本身出错就是最高等级的缺陷**——它不会让测试变红,它让"绿"或"红"
这两个字失去含义。

到 0.7.26 为止,本项目已经踩中守卫失效的**全部四种形态**,其中两种是在 2026-09
的工程体检与合并前复核中才发现的,分别潜伏了八天和三周。AGENTS.md 原有的规则
"写完守卫必须用变异确认它真的会红"只覆盖其中两种;本文记录另外两种长什么样。

## 当前结论:四种失效,以及它们在 CI 上的样子

| 失效类型 | 守卫出了什么错 | 在 CI 上看起来 | 靠什么抓 |
|---|---|---|---|
| **空转** | 什么也没断言到 | 恒绿 | 变异 → 必须红 |
| **错靶** | 匹配到名字或注释,不是干活的那一行 | 恒绿 | 在**调用点**变异 → 必须红 |
| **恒红** | 前提在任何提交上都不成立 | 恒红 | 还原 → **必须绿** |
| **被遮** | 守卫本身没错,但排在会失败的步骤后面 | 根本不出现 | 核对哪些步骤**真的执行过** |

**这张表的要点在第三行。** 变异验证对恒红的守卫照样"通过"——它确实会红,而且
是每次都红。一个恒红的守卫和一个严格的守卫,从变异实验的结论上看**一模一样**。
唯一能把它们分开的问题是反过来问的:*没毛病的时候,它会不会变绿?*

第四行则连红都不给。`set -e` 下,排在失败步骤之后的检查不会执行,也不会报告
自己没执行;它在日志里的样子和"不存在"完全相同。

## 双向验证规程(contains: procedure)

写完或改完一个守卫,按顺序做完这六步,缺一步都可能留下上表里的某一类:

1. **先确认变异真的注入成功**,再看结论。替换锚点跨行未匹配会让变异变成空
   操作,于是守卫"通过"其实什么都没证明(2026-08-24 实际踩到)。
2. **变异 → 必须红。**
3. **还原 → 必须绿。** 这一步是 0.7.26 之后补进来的;0.7.16 与 0.7.26 两次
   事故都只做了第 2 步。
4. **扫源码文本的守卫,要匹配那一行真正干活的代码**,不是名字、不是注释里的
   同名词句。
5. **归一化要覆盖整个不可复现面,不能只归一化其中一半**;而且**归一化器匹配
   不到目标时要报错,不能放行**——否则它会安静地拿错误的文本去比对。
6. **断言取精确值而非下限。** 下限挡不住"新加一条、旧的一条悄悄不跑"这种
   总数不变的漂移(端上条数断言即按此设计)。

流水线一侧还有两条:

7. **关键检查不要排在可能失败的步骤后面。** 便利性步骤(artifact 上传)标
   `continue-on-error`,或移到门禁之后。
8. **读"哪些步骤真的执行了",不要只读 job 的红绿**;并且**每一条 lane 都要
   有人看**。当年推送线有徽章而 schedule 线没有,这正是它能红三周的原因;
   观测面本身如何失效,见下文「流程层的第四类」。

## 案例台账(contains: experience)

**空转**

- `test_cal_deps.py` 的计划检查只断言"不抛错",`validate_order` 退化成空操作
  照样通过。0.7.23 补上反向断言:反转后的计划**必须**抛错。
- **0.7.31**:音计划的"导频精确落位"测试在标准计划下**两条路径同解**——模型活跃集
  就是标准占用集,所以 rank 映射与直接查表结果相同,关掉直接分支测试照样绿。断言是
  真的,但对它声称的那条性质是空转的。改用打孔计划区分才判红。**教训:一条断言为真
  不等于它在检验你以为的那件事;变异是唯一能分开这两者的办法。**

**错靶**

- 0.7.5 的手机陷阱守卫初版用 `index("Python.start")` 找调用点,先匹配到的是
  作者写在上一行注释里的同名文字;删掉真正的调用,守卫照样绿。改为匹配真实
  调用行后才成立。
- 同一类连踩四次:`Python.start`、`annotation_typing=False`、
  `_drop_package_initializers`(见 `cairn/Android出货线.md`)。

**恒红**

- **0.7.16(红八天)**:自检要求金标差值**精确为 0**。GitHub 托管 runner 的
  CPU 型号不固定,CPU/BLAS 求和顺序读出 7.1e-15 dB;numpy/scipy 与本地同版,
  排除版本差异。被推翻的假设:"金标在生成它的同一套 numpy 上必然位级复现"
  ——只对同一台机器成立。修法:断言改 `<= 1e-9`(测的是比较逻辑,不是位级
  复现)。
- **0.7.26(红三周)**:文档陈旧检查比对"已提交的 HTML"与"当场重建的 HTML",
  只归一化 ISO 时间戳,**把 provenance 页脚里的 commit id 留在比对文本里**。
  一个文件不可能记录包含它自己的那个提交——已提交的副本生成于父提交(工作树
  dirty),重建盖的是 HEAD——所以它**在任何提交上都不可能绿**。修法
  `tools/docs_staleness.py` 整体去掉页脚,并在页脚不匹配时报错而非放行。

**被遮**

- **0.7.26**:原理图检查 `tools/build_assets.py --check` 排在上面那条陈旧检查
  之后,`set -e` 下**从未执行过**。现场核查:它本身是干净的(4 张图最新),
  所以没有真实漂移被漏掉——漏掉的是**发现漂移的能力**。
- **0.7.6 时期**:APK 上传撞 GitHub 账户级存储配额,而上传步骤排在模拟器之前,
  编译版**唯一的物理检查被跳过**,job 红的原因却是存储记账。修法:上传移到
  端上门禁之后并标 `continue-on-error`,保留期 14 → 7 天。

## 被推翻的判断(contains: lesson)

- 工程体检报告 P0-1 说"修掉金标断言后,覆盖率、文档陈旧、原理图三步就会真正
  执行"。**只对第一步成立。**后两步直到 0.7.26 才第一次跑起来——体检当时读的
  是 push lane 的结论,而这三步在 schedule lane 里。更正记录在
  `docs/backlog_zh.md` B26。
- "CI 绿"在 0.7.16 之前一直只指 `android.yml`;0.7.16 之后只指 `ci.yml` 的推送
  快线。**"CI 绿"始终是一个需要追问"哪条 lane"的说法。**

## 流程层的第四类:观测面(0.7.28 处置)

守卫可以完全正确而仍然没人知道它红了。夜间 lane 红三周,直接原因是没人看它,
而"没人看"是三件事叠出来的:

- **全量 lane 只在 schedule 上跑**,而 schedule 在凌晨、在没人面前。**处置:
  让 full lane 也在 push 上跑。**这是消掉不对称性,不是再加一个探测器;cron
  保留,职责收窄成"没有推送的日子里抓环境漂移"。代价实测:推送线总时长
  3 分 26 秒 → 4 分 50 秒,约 +1 分 24 秒。**事前估成"约半分钟"是错的**——
  当时比的是同一次运行里两个并行 job 的差,而墙钟由最慢的那个决定。这是本文
  反复说的那件事的又一例:**估的数和测的数不是一回事,写进文档的必须是测的
  那个。**
- **徽章不分触发方式**。不带 `?event=` 的徽章只反映该 workflow **最近一次**
  运行:推送频繁且绿,于是徽章长绿,而每一次 schedule 都红。0.7.16 加徽章的
  本意正是"让红可见",它加了,但看的是错的那条 lane。**处置**:按 `event`
  拆成 push / schedule / android-dispatch 三个。
- **schedule 失败没有落点**。GitHub 会给最后改 cron 的人发邮件,但那条链路从
  仓库里看不见也验不了。**处置**:cron 失败时开一个 issue(已存在则追评论,
  不重复开)。

### 又一次"空转"——这次是仪表盘(contains: lesson)

拆徽章时的第一版想写 `?branch=main&event=schedule`。**先查了运行条数才发现**
`main` 上的 schedule 与 workflow_dispatch 运行都是 **0 条**——因为仓库默认分支
仍是开发分支,而定时与手动派发**只在默认分支上触发**。那样写会做出两个永远
空白的徽章:一个恒不报告的仪表盘,和一个恒不断言的测试是同一类东西,而且更
难发现,因为空白徽章看起来只是"还没跑"。**教训:给任何按条件过滤的观测面下
结论前,先确认该条件下真的存在数据。**

## 开放问题(contains: open_question)

- cron 失败开 issue 这条**尚未端到端验证**:它只在真实的定时失败时才会触发,
  而验证它要么推一个故意坏掉的提交,要么在仓库里真开一个 issue。目前只验证了
  YAML 解析与条件表达式。首次真实触发时需要确认它确实开了 issue。
- ~~仓库默认分支仍是开发分支~~ **已处置(2026-09-11,0.7.29):默认分支切到
  `main`。**同批复核结果见下节。

## 默认分支切到 main 之后(0.7.29)

切换的理由是分工变干净:**push 覆盖会动的那条分支,cron 覆盖不动的那条**。此前
两者都盯着同一棵会动的树,cron 因此几乎没有独有价值;现在它对着固定的 `main`,
红了就只能是环境变了。次要理由是 fresh clone 落在 `main` 而不是一个会话名分支上。

切换会连带改动三件事,同批处理:

- **`android / golden` 徽章会立刻变空白。** 它按 `event=workflow_dispatch` 过滤,
  而派发只在默认分支触发;实测 `main` 上的派发运行是 **0 条**。处置:唯独给它和
  `ci / dev` 钉 `branch=`。**这与 0.7.28 写下的"都不带 `branch=`"相反——当时那句
  在当时的默认分支下是对的,默认分支一变就不对了。规则跟着前提走,前提变了要复核。**
- **手动派发的默认 ref 变成 `main`。** 金标裁决必须派发被测提交所在的分支,而
  Run workflow 的默认值现在会去裁决 `main`;当 `main` 恰好等于开发分支顶端时,
  这次误派发**在结论上完全看不出来**——版本核对、金标对拍、导出检查全会通过,
  只是测的不是你以为的那棵树。已写进 `AGENTS.md` 与 `cairn/Android出货线.md`。
- **cron 移到 `main`。** 如上,这是改善;代价是 `ci / nightly` 徽章在切换后的
  **第一次 cron 之前是空白的**(`main` 上此前没有定时运行)。这是一次性的过渡态,
  不是缺陷,但正因为空白和"坏了"看起来一样,写在 README 里说明。

**一个低信息量但不算缺陷的仪表盘**:`ci / main` 近乎恒绿——`main` 只从已裁决为绿
的提交快进而来,所以它回答的是"主干跑过且过了",不是"主干挡住了什么"。留着它是
因为交付方打开 README 时想看的就是这个;但不要把它当成守卫。

## 指针

- 规则:`AGENTS.md`「验证」节(变异先确认注入、双向验证、匹配干活的那一行)。
- 代码:`tools/docs_staleness.py`、`scripts/ci_fast.sh` 与 `scripts/ci_nightly.sh`
  (逐项收集失败、便宜的检查排前面)、`.github/workflows/ci.yml`(full lane 上
  push、cron 失败开 issue)、`README.md`(按 `event` 拆开的**四个**徽章,其中两个
  钉 `branch=`)、`tests/test_docs_build.py`(陈旧判定逻辑的三条守卫)、
  `tests/test_import_layering.py`、`tests/test_android_bridge.py`(调用面)。
- 记录:`docs/backlog_zh.md` B16 / B26 / B27 / B28;`cairn/LOG.md` R34–R37;
  CHANGELOG 0.7.5、0.7.16、0.7.23、0.7.26、0.7.28、0.7.29;
  `cairn/Android出货线.md` 坑点清单与派发协议。
